description = "Pause future autonomy queue claims."

prompt = """
Use the `autonomy-queue` skill with operation `Stop`.

If Gemini CLI does not auto-load the project skill, read:
@{.agents/skills/autonomy-queue/SKILL.md}

Runtime name: Gemini CLI.

User arguments: {{args}}
"""
